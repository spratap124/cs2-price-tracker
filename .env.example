# Environment Configuration Example
# Copy this file to .env.development or .env.production and fill in your values

# Node Environment (development or production)
NODE_ENV=development

# Server Configuration
PORT=3001

# MongoDB Connection
# Get your connection string from MongoDB Atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cs2-price-tracker?retryWrites=true&w=majority

# CORS Configuration
# Add allowed origins separated by commas
# Development: http://localhost:5173,http://localhost:3000
# Production: https://suryapratap.in,https://cs2-api.suryapratap.in
ALLOWED_ORIGINS=

# Price Provider Configuration
# Options: "steam" or "skinport"
# Steam: Free but has rate limits
# Skinport: More reliable, requires API key
PRICE_PROVIDER=steam

# Use fallback price provider if primary fails
USE_PRICE_FALLBACK=true

# Steam API Configuration
# Minimum time between requests in milliseconds (default: 10000 = 10 seconds)
STEAM_API_MIN_INTERVAL_MS=10000

# Currency code (24 = INR, 1 = USD, 2 = GBP, etc.)
# See Steam currency codes: https://steamcommunity.com/dev/registerkey
CURRENCY=24

# User agent for API requests
USER_AGENT=PriceTracker/1.0

# Skinport API Configuration (optional)
# Get your API key from https://skinport.com (Settings → API)
# SKINPORT_API_KEY=your_skinport_api_key_here
# SKINPORT_API_MIN_INTERVAL_MS=37500

# Cron Job Configuration
# How often to check prices in minutes (default: 5)
CHECK_INTERVAL_MINUTES=5

# Discord Webhook (optional)
# Create a webhook in your Discord server settings
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Debug Flags
# Set to "true" to enable debug logging for specific components
DEBUG_STEAM=false
DEBUG_SKINPORT=false
DEBUG_PRICE_PROVIDER=false
